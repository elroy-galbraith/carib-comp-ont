# Caribbean Compliance Ontology — Python dependencies
#
# Core (required for all scripts)
anthropic>=0.25.0       # Haiku API calls in extractor.py
PyYAML>=6.0             # YAML frontmatter in to_turtle.py / extractor.py
pyoxigraph>=0.3.22      # In-memory SPARQL store in load_to_oxigraph.py

# Curator agent
watchdog>=4.0.0         # inbox/ directory watcher in curator.py
gitpython>=3.1.40       # Git branch + commit operations in curator.py

# PDF text extraction (install at least one)
docling>=2.0.0          # Preferred: layout-aware PDF → Markdown (IBM)
pdfplumber>=0.10.0      # Fallback: simpler PDF text extraction
