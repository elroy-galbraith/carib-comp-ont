"""kgforge — configurable PDF → typed-entities → graph → query platform.

Phase A: only the pack/ subpackage is implemented. Existing scripts in
scripts/ load a DomainPack and source their hardcoded constants from it.
Subsequent phases add engine/, approval/, project/, ui/, cli/.
"""
